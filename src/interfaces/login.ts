export type Login = {
  secretAccessKey: string;
  accessToken: string;
  username: string;
  region: string;
};

export interface ILogin {
  secretAccessKey: string;
  accessToken: string;
  username: string;
  region: string;
}
